
Hs = 1;
fs = 1000;

syms s z

T = 1/fs;

% Bilinear transform:
% s = (2/T)(z - 1)/(z + 1)
sBilinear = (2/T)*(z - 1)/(z + 1);

% Substitute into the analog transfer function
H_z = simplify(subs(Hs, s, sBilinear));

save('bilinear_conversion.mat', 'H_z')

end